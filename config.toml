base_url = "/"

title = "The SILE Typesetter"

compile_sass = false
build_search_index = false

generate_feed = true
feed_filename = "feed.xml"

[slugify]
paths = "safe"
taxonomies = "safe"
anchors = "safe"

[markdown]
highlight_code = true

[extra]
